<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><% if(editing) { %> Edit your Home <% } else { %>
    Add Your Home
    <% } %>
  </title>
  <link rel="stylesheet" href="/styles/output.css">
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100  items-center justify-center min-h-screen">

  <%- include('../partials/nav') %>

 
  <div class="bg-white p-8 rounded-lg shadow-lg w-full max-w-md">
    <h1 class="text-3xl font-extrabold text-yellow-600 mb-8 text-center">Add Your Home</h1>
    <form action="/host/<%= editing ? 'editHome': 'addHome'%>" method="POST" enctype="multipart/form-data" class="space-y-6">
      <div>
        <input type="hidden" name="id" value="<%= editing ? home._id : '' %>">

      <label for="houseName" class="block text-lg font-medium text-gray-700 mb-2">Name:</label>
      <input type="text" name="houseName" 
      placeholder="Enter your House name"
      value="<%=editing ? home.houseName : '' %>"
      id="houseName"  class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-yellow-600 focus:border-transparent">
      </div>
      <div>
      <label for="price" class="block text-lg font-medium text-gray-700 mb-2">Price:</label>
      <input type="number" name="price" id="price"
      placeholder="Enter the price" 
      value="<%=editing ? home.price : '' %>"
       class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-yellow-600 focus:border-transparent">
      </div>
      <div>
      <label for="location" class="block text-lg font-medium text-gray-700 mb-2">Location:</label>
      <input type="text" name="location" id="location" 
      value="<%= editing ? home.location : '' %>"
      placeholder="Enter the location" class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-yellow-600 focus:border-transparent">
      </div>
      <div>
      <label for="rating" class="block text-lg font-medium text-gray-700 mb-2">Rating:</label>
      <input type="number" name="rating" id="rating" 
      value="<%= editing ? home.rating : '' %>"
      placeholder="Enter the rating" class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-yellow-600 focus:border-transparent">
      </div>
      <div>
      <label for="photoUrl" class="block text-lg font-medium text-gray-700 mb-2">Photo URL:</label>
      <input type="file" name="photo" id="photoUrl" placeholder="upload the photo URL"
      value="<%= editing ? home.PhotoURL : '' %>"
      class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-yellow-600 focus:border-transparent">
      </div>
      
      <button type="submit" class="w-full bg-yellow-600 text-white py-3 rounded-lg hover:bg-yellow-700 transition duration-300 font-semibold">
        
        <% if(editing) { %> Update Home <% } else { %>
          Add Home
          <% } %>
        </button>
    </form>
  </div>

</body>
</html>